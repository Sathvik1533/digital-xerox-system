# Infrastructure

Docker and AWS deployment configuration:

- `Dockerfile` — container definition (coming in Phase 9)
- `task-definition.json` — ECS task definition (coming in Phase 10)
- `cloudwatch.json` — CloudWatch log group config
