# Scripts

Utility scripts for local development and deployment:

- `setup_dynamodb_tables.py` — creates DynamoDB tables locally or in AWS (coming in Phase 6)
- `seed_data.py` — seeds sample data for local testing
